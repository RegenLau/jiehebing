# 患者端待开始服药过渡页验收

final result: passed

## 范围与视觉依据

- 视觉真源：`/Users/regenlau/.codex/generated_images/01a08a40-f58b-79d0-a91f-f5940edea040/exec-b691b008-8a7b-4aa6-b6e1-16d325582a6b.png`，853 × 1844 像素。
- 实现地址：`http://127.0.0.1:3006/#/patient-app`，待开始服药演示患者 `13910001029`。
- 实现截图：本轮 Codex in-app Browser 的 375 × 812 CSS 像素实时截图；浏览器截图未另外落盘，页面已保留为可检查的交付标签。
- 密度归一：参考图按 853:1844 比例缩放到 375 CSS 像素宽后比较。参考图含 iOS 状态栏和微信胶囊；实现沿用项目移动 Web 容器，不在业务页面伪造宿主控件。

## Findings

- 没有待修复的 P0、P1 或 P2 问题。确认标题、状态插图、主副标题、开始时间卡片、状态标签、提示话术和底部说明与视觉真源保持同一内容、顺序与视觉层级。
- P3：实现插图的可见主体略小于参考图，但标题和时间卡片的首屏位置保持一致，不影响识别和信息获取。
- P3：实际日期由患者方案动态显示；本轮固定演示账号显示 `2026年9月11日 上午 08:00`，参考图中的 `2026年9月17日` 仅为设计示例。

## 五项视觉检查

1. 字体与层级：标题、状态说明、日期和辅助话术层级清楚，中文无异常换行或截断。
2. 间距与布局：375 × 812 视口中全部核心信息首屏可见，无横向溢出；底部没有首页导航或可绕过等待状态的操作入口。
3. 色彩：浅蓝页面底色、蓝色日期、浅灰状态标签和提示卡片与参考图一致，正文对比度清晰。
4. 图像与图标：日历、确认标记和时钟使用本地透明 PNG，边缘清晰、无占位框；提示图标使用项目现有图标库。
5. 文案与内容：页面完整显示开始服药时间及确认后的等待话术；日期、时段和状态均来自接口数据。

## 功能与运行证据

- 固定 Mock 患者 `13910001029` 已完成身份和药品确认，开始服药时间按服务启动日期自动设为次日 08:00；Mock 重启后仍可重复进入该状态。
- 接口实测返回 `stage: pending_start`，并拒绝首页请求，提示“用药计划尚未开始，请在开始服药时间后进入”。
- 浏览器真实渲染显示动态时间 `2026年9月11日 上午 08:00`，页面无首页底部导航。
- Mock API 39/39 项测试通过；前端完整构建与 TypeScript 检查通过；差异空白检查通过。

## Implementation Checklist

- [x] 已确认患者在开始服药时间前进入专用过渡页。
- [x] 过渡页显示动态开始日期、时段和确认话术。
- [x] 等待期间无法进入首页、用药页或任务页。
- [x] 到达开始时间后，接口状态自动切换为首页。
- [x] 固定演示患者及登录页快捷填入入口已加入。

---

# 患者端服药打卡页验收

final result: passed

## 范围与视觉依据

- 视觉真源：`/Users/regenlau/Desktop/用药方案-服药打卡.png`，750 × 1624 像素，按 @2x 视为 375 × 812 CSS 像素。
- 实现地址：`http://127.0.0.1:3006/#/patient-app`，演示患者 `13910001019` 的“用药 > 服药打卡”。
- 最终浏览器截图：`/tmp/patient-medication-restored-final.png`，659 × 827 像素；患者端内容裁切：`/tmp/patient-medication-restored-app.png`，390 × 827 像素，DPR 1。
- 完整同图对比：`/tmp/patient-medication-comparison-restored-final.png`；重点区域同图对比：`/tmp/patient-medication-comparison-focus-restored-final.png`。
- 密度归一：参考图保持比例缩放为 390 × 844 像素；实现按 390 × 827 原始内容裁切，并在对比画布底部补 17 像素，不拉伸页面。参考图含 iOS 状态栏、微信胶囊和底部安全区，实现沿用项目现有移动 Web 容器，不把这些宿主控件画进业务页面。
- 对比状态存在业务数据差异：参考图是 08:41、早晨 07:00、3 种示例药；实现截图是当天下午、12:00 与 19:00、患者所属方案的 2 种药。两者都处于“只展示当天待服药”的状态，结构、层级、控件与交互可直接比较，不做药名和时间的像素级一致声明。

## Findings

- 没有待修复的 P0、P1 或 P2 问题。标题、说明、分段切换、下次服药、完成进度、时段标题、服药时机、状态标签、时间线、药品卡和底部菜单与参考图保持同一信息结构和视觉层级。
- P3：实现使用项目现有中文系统字体、Iconify 图标和通用药品包装图，参考图的系统字体、微信宿主控件及具体包装略有差异；这些差异不影响页面识别、信息层级或核心操作。
- P3：真实药品规格比参考图的 `500mg` 更长，在窄卡片中单行省略；药品名称与每次剂量仍完整可读，避免为追求示例图长度而改写业务数据。

## 五项视觉检查

1. 字体与层级：中文无衬线字体、28px 页面标题、16px 药名与按钮、13–15px 辅助信息层级清楚；长规格单行省略，没有标题或按钮换行挤压。
2. 间距与布局：16px 页面边距、圆角分段控件、72px 汇总卡、时段间距、时间线与 44px 最小打卡按钮符合移动端触控要求；390px 内容区没有横向溢出，底部菜单持续可见。
3. 色彩：主色蓝、浅蓝底、橙色待服药、灰色不可操作状态与参考图语义一致，进度环补齐后汇总区比例一致。
4. 图像与图标：药品包装使用生成后的本地 PNG；时钟、时间线与底部导航使用现有图标库，资源清晰，无占位框、文字图标或失效图片。
5. 文案与内容：标题及副标题与参考图一致；页面只渲染当天尚未完成的药品，已完成时段只进入顶部统计，不显示历史列表或更正入口。

## 比较历史

- 第 1 轮：`/tmp/patient-medication-comparison.png` 显示汇总卡缺少完成进度环，药品卡右侧比参考图更贴边，记为 P2。
- 修复：使用项目已有 `ElProgress` 补充圆形进度，按已完成时段/今日总时段计算百分比；药品列表右侧增加 10px 留白。
- 第 2 轮：`/tmp/patient-medication-comparison-restored-final.png` 与重点区域对比均未发现新的 P0/P1/P2；进度环、分隔线、卡片宽度与参考图结构一致。

## 功能与运行证据

- 接口返回的 `slots` 日期集合只有 `2026-09-10`，包含 07:00、12:00、19:00；初始状态分别为 completed、pending、pending，顶部统计为今日 3 次、已完成 1 次、下次 12:00。
- 浏览器实际点击验证：打卡一个药品后该药品立即消失；同一时段两种药均打卡后，12:00 整个时段消失，统计更新为已完成 2 次、下次 19:00。完成记录没有重新出现在患者端。验证后已重启 Mock，恢复初始演示数据。
- 最终重启后检查到的浏览器日志为 0 条 error、0 条 warning；开发过程中曾有热更新尚未完成时的旧错误，不作为最终状态证据。
- 前端完整构建与 TypeScript 检查通过；Mock API 38/38 项测试通过；本次服药打卡改动在独立检查时通过 ESLint、Prettier 和 Stylelint，Node 语法与差异空白检查通过。

## Implementation Checklist

- [x] 仅展示当天待服药时段和未打卡药品。
- [x] 已完成记录只计入汇总，不在患者端展示。
- [x] 演示患者具备早、中、晚 3 个时段及可见待服药数据。
- [x] 单药打卡、整时段完成、未来时段禁用和数据恢复均已验证。
- [x] 完整视图与重点区域经过同图对比，最终无 P0/P1/P2。

---

## 历史验收：用药方案与分组药品卡片

final result: passed

## 范围与视觉依据

- 参考图 1：`/var/folders/53/m2_1l3fd2rl09v2mhw2nmx540000gn/T/codex-clipboard-e25e74ba-d04f-4d57-a38a-62c7499076f6.png`，1868 × 668 像素。
- 参考图 2：`/var/folders/53/m2_1l3fd2rl09v2mhw2nmx540000gn/T/codex-clipboard-551b207c-2854-4f88-9d88-cb6226a4ad64.png`，1862 × 932 像素。
- 实现：`http://127.0.0.1:3006/#/medication-plan/schemes?mode=edit&id=1` 及分组详情的用药方案标签。
- 最终截图：`output/playwright/medication-scheme-upload.png`、`output/playwright/medication-scheme-card.png`、`output/playwright/group-medication-narrow.png`。
- 桌面 CSS 视口 1280 × 720，DPR 2；浏览器输出图片为归一化后的 1280 × 720。窄屏 CSS 视口 768 × 1000，浏览器截取内容区为 760 × 990 像素，验收后已恢复默认视口。
- 两张参考图与两张桌面实现截图在同一次图像输入中共同检查。参考图是独立面板，实现是含后台导航的完整视口，按面板内容宽度和字段相对位置比较，不把外围导航或图片密度差异当作缺陷。没有进行像素级一致声明。
- 上传面板及卡片主字段在各自截图中清楚可读，已单独滚动到这两个区域抓图，无需再次裁切放大。

## 最终交互与内容状态

- 根据用户后续确认，上传处方图片与关键词搜索放在用药方案管理的新增/编辑页面。分组页面只有关键词搜索入口，实际页面上传标签数量为 0。
- 上传区域比较状态：未选择图片，识别按钮禁用。卡片比较状态：待确认、每日 1 次、提醒时间与服药时机可编辑。
- 图 2 的头孢示例替换为该项目真实核对规格的结核病药品，不借用参考图的药品名称或剂量。截图中利福平胶囊显示 `0.3g×50粒/瓶`、计量单位为粒。
- 药品量是方案默认首次发药量，不能标为患者个人剩余药量；新增预计可用天数，并保留现有注意事项字段。治疗天数保留，周期按药量自动计算。

## 五项视觉检查

1. 字体与层级：使用后台现有中文无衬线字体与 Element Plus 字体设置；卡片标题、字段标签、主次文案层级清楚，名称/规格可读。沿用后台字体，不替换全局主题。
2. 间距与布局：对应参考的顶部状态与操作、2:1:1 药品信息列、剂量行、整行频次、提醒行。独立边框、浅灰字段背景及分节留白保留。补充说明位于主要输入之后。768 宽下布局折行，编辑区域 clientWidth/scrollWidth 均为 630，无区域横向溢出。
3. 色彩：主操作及选中项沿用项目主色，待确认橙色、删除红色，次级说明灰色。与参考纯蓝存在轻微差异，为后台主题一致性的有意适配。
4. 图像与图标：参考仅有标准图片/上传图标，无需要生成的产品插图；使用现有图标库。选择图片后预览保持比例。本地识别说明明确，不暗示真实 OCR。
5. 文案与数据：上传步骤、确认/删除、每日 1–4 次、餐前/餐后/睡前/空腹均可辨认；已移除规格占位文字。规格、厂家资料来源记录在 `mock-api/README.md`，模拟用量不作为临床处方。

## 功能与回归证据

- 浏览器验证搜索添加、防重复添加、删除确认、图片选择/预览、模拟识别生成待确认卡片、未确认阻止保存。
- 验证频次 2 → 3 后生成不重复的 07:00、12:00、19:00。30 / 2 / 3 = 5 天的周期在分组保存后重新打开保持。
- 用药方案管理保存后回到列表、版本更新，再打开时规格及卡片数据保持。方案更新不改写既有分组快照的行为由接口测试覆盖。
- 完整前端构建通过；35 项 Mock 测试通过；测试覆盖自定义分组、方案卡片、确认约束、非法时间/重复药品、取药计算、图片来源、患者计划的时机继承。
- 新验收标签的日志中存在一条 06:36:44 的 `[ScriptError] Object`，无可展开详情；之后的搜索、保存/重开与布局检查未出现新错误。没有把日志检查描述为全程零错误。
- 初次窄屏抓图处于侧栏折叠动画中，不作为最终证据；等待稳定后重抓，侧栏收起、内容完整。该过程无需修改全局导航。

## 结论与余项

没有待修复的 P0/P1/P2 视觉问题。标准图标与主色的细微差异作为 P3，沿用后台主题。已按最新需求完成页面入口分工，无额外部署或真实识别服务接入。

验收清单：页面位置、卡片状态、频次提醒、规格数据、保存重开、周期计算、桌面/窄屏显示均完成。
