# 结核病患者管理 · 本地模拟环境

当前管理端完全使用本地 Mock 数据。默认命令不启动 `backend/` 中的 PHP 服务，不连接原数据库、Redis、OSS、AI 或微信服务。

## 启动

需要 Node.js 22 或以上、pnpm 8.8 或以上。在项目根目录执行：

```bash
pnpm install:all
pnpm dev
```

打开 http://127.0.0.1:3006 。演示账号 `admin`，密码 `Mock123456`，验证码 `1234`。

患者端入口为 http://127.0.0.1:3006/#/patient-app 。可用手机号：正常流程 `13910001019`、待开始用药 `13910001029`、项目已结束 `13910001030`。

- 前端：`127.0.0.1:3006`；Mock：`127.0.0.1:3010`。
- 浏览器业务请求统一经过 `/api`，开发及构建预览只转发到本地 Mock。接口不存在直接报错，服务不可用时不会切换原后台。
- `Ctrl+C` 停止本次启动的两个服务。端口占用会明确报错，不自动选择另一个端口或结束其他项目。
- 页面刷新保留本次修改；重启服务恢复初始数据，并清空上传文件及登录会话，需要重新登录。
- 模拟数据以服务启动时的上海日期为基准；持续跨日演示时重启即可重新生成。

## 构建与检查

```bash
pnpm check        # Mock 接口测试、构建、TypeScript 检查
pnpm build        # 生成 frontend/dist；先由 Vite 生成自动导入类型，再执行类型检查
pnpm preview      # 同时启动构建预览与 Mock，地址仍为 127.0.0.1:3006
pnpm mock:check   # 仅接口测试，使用独立随机端口及固定测试时间
```

构建预览前先停止开发服务。`frontend/` 内的 `dev`、`build`、`serve` 也默认采用 `mock` 模式，但单独启动前端时需另行启动 `mock-api`。推荐使用根目录命令，统一管理进程。

保留原生产配置及 GitLab CI 供查阅，本次没有部署或修改原后台。不要将当前模拟账号及内存服务作为正式生产后台使用。

## 项目与数据说明

- [项目结构、业务关系、页面与接口映射](docs/PROJECT.md)
- `frontend/`：现有 Vue 管理端及组件；只修复本次运行所需的配置、公共入口和类型问题。
- `mock-api/`：Node HTTP 服务、关联模拟数据、接口测试；不会执行 PHP 或种子 SQL。
- `backend/`：原服务源码，作为接口契约与业务逻辑参考，不参与本地运行。

覆盖医生端业务菜单、研究与分组、通用方案、提醒方案、患者执行与工作台。患者、个体用药、服药记录、任务、答卷、每日反馈、报告、不良反应和统计共用同一份本地数据；导出为真实 Excel 文件，上传资料保存在 Mock 内存并通过同源地址访问。

管理端支持研究、分组、用药方案、问卷、任务模板、提醒方案、患者连续建档、个体用药和发药，并可跟进用药、任务、反馈、报告及不良反应。`#/patient-app` 提供患者端移动页面，含后台手机号准入、首次双确认、首页/用药/任务/我的四菜单、当天待服药逐项打卡（已完成记录不在患者端展示）、每日反馈、随访问卷、报告上传与核对、不良反应上报、余药估算、提醒偏好、联系信息和健康科普。项目状态按研究周期自动计算，也可由研究人员手动结束；没有可参与项目的患者登录后只显示“项目已结束”页面。每日反馈包含体重下降及浏览器语音转文字/文本降级；报告上传有拍摄提示、预览与删除，患者核对保留指标单位、参考范围和可选异常标识；药品卡可展开用药指导。问卷统计已区分答卷份数、参与患者数和题答案行数。

## 验收记录

2026-09-08 本地实际验证：

- `pnpm install:all` 安装成功；`pnpm check` 通过全部 12 项 Mock 测试、Vite 构建及 TypeScript 检查。改动文件 ESLint 与 `git diff --check` 通过。
- 接口测试覆盖登录成功与失败、验证码、会话失效、管理员重名及改密、分页筛选、空数据及不存在的 ID、文章保存、药品启停、问卷删除约束、上传、导出和重启重置；使用固定时间重算工作台统计及历史逾期待办。
- 构建预览中实际登录并遍历 8 个业务菜单，无页面脚本异常或失败请求；检查患者详情五个标签及答卷、管理员新增、个人资料保存与刷新、日志、清缓存、文章新增与图片上传/图库插入/详情预览，以及用药列表路由参数切换后的刷新。
- 从页面下载的问卷及不良反应文件均能解析为 XLSX，分别含 8 行答卷和 36 行报告，中文表头与内容正常，导出覆盖全部数据而非当前页；接口测试另验证筛选导出。
- 观察到业务请求均访问本地 `/api`。停止 Mock、单独运行前端时，`/api/health` 返回 HTTP 500，代理明确报告 `ECONNREFUSED 127.0.0.1:3010`，未回退原后台。统一入口停止后两个端口均释放；端口占用时明确失败且不影响已有服务。
- 重启后旧浏览器会话被拒绝并返回登录页；默认演示账号可重新登录。最终服务恢复初始模拟数据，测试新增的账号、文章和上传资源不保留。

验收截图、浏览器记录和下载样本保存在本地 `output/playwright/`（不提交 Git）。项目说明与接口表见上方文档。

2026-09-10 提醒方案配置验收：新增提醒方案维护入口，小组可一次关联服药、随访任务和取药提醒规则；任务不再逐条重复勾选提醒阶段。方案更新不会自动覆盖小组快照，小组页面会提示并由用户主动更新。Mock 30 项测试、前端构建与 TypeScript、相关文件 ESLint 和常用视口页面复审均通过，详见[第一版实施与复审记录](docs/第一版实施与复审记录.md)。

2026-09-10 患者建档与个体用药验收：可从患者列表或研究分组进入三步连续建档，一次保存档案、研究归属、个人用药方案和可选的首次发药。默认采用分组方案，医生主动开启后可单独调整当前患者的治疗天数、组内药品启停、单次用量及服药时间；患者列表直接显示安排状态。Mock 31 项测试、前端构建与 TypeScript、相关文件 ESLint 和常用视口页面复审均通过，详见[第一版实施与复审记录](docs/第一版实施与复审记录.md)。

2026-09-10 患者首次登录与双确认验收：患者端使用后台登记手机号登录，先核对身份，再逐项核对当前用药的数量、规格、用量和时点；资料有误或用药有疑问会回传医生端。医生修改对应内容后旧确认自动失效。Mock 32 项测试、移动视口主流程与医生端联动均已检查，详见[第一版实施与复审记录](docs/第一版实施与复审记录.md)。

2026-09-10 第一版业务闭环复审：补齐患者四菜单、任务安排弹窗、用药时点整次确认及更正、多轮问卷、每日反馈、报告上传/核对/补充、不良反应上报与人工跟进、余药估算，以及按研究/分组筛选的工作台和导出。`pnpm check` 完整通过，包含本地 Mock 32/32 项测试、Vite 构建和 TypeScript 检查；本次变更文件的 ESLint 和差异空白检查也已通过。患者端与后台关键页面已重新检查，用药计划页的标题挤压已修正。详见[第一版实施与复审记录](docs/第一版实施与复审记录.md)和 [页面设计复审](design-qa.md)。

2026-09-10 完成性审计：进一步补齐会议明确的体重变化采集、可操作的语音转文字入口、报告指标单位/参考范围/异常标识、上传提示与文件预览/删除，以及药品卡内用药指导。移动窄屏复核了每日反馈、报告上传空态、指标核对、异常标识保存与详情、用药指导展开；医生端复核了异常标识核对列。接口测试继续保持 32/32 通过。真机语音权限、拍照/相册选择和真实 OCR 仍按下方外部边界验收。

2026-09-10 项目状态与患者结束页验收：项目列表按上海日期自动显示未开始、进行中和已结束，三种标签使用不同颜色；状态弹窗只允许手动结束并提示患者端影响。自然结束项目延长结束日期后重新计算，手动结束保持优先。新增已结束项目和患者 `13910001030`，患者登录后进入无底部导航的结束页，业务接口返回项目已结束。Mock 41/41 项测试、构建、TypeScript、本次文件 ESLint/Stylelint/Prettier 和 375×812 及桌面页面复审通过。

2026-09-10 空项目与空分组删除验收：项目只有在没有分组和关联患者时可删除，分组只有在没有患者时可删除；存在空分组的项目需先处理分组，不做级联删除。已配置但未使用的空分组可以删除，通用方案和模板不受影响。Mock 42/42 项测试、构建、TypeScript、相关文件静态检查及浏览器新建—删除流程均通过。

2026-09-10 检查任务与报告提交合并验收：周期任务模板移除“报告提交”，检查任务统一承载报告上传、患者确认、医护核对和补充流程；独立索取历史或缺失资料时，由医生临时创建“补交检查资料”任务。检查报告继续作为独立记录保留附件、指标、版本和核对历史。Mock 42/42 项测试、构建与 TypeScript 检查通过；实际页面确认模板下拉仅显示检查、复诊、取药、其他，并显示检查任务闭环说明。

已知边界：患者端当前是同一 Vue 工程内的移动 Web 入口，便于在第一版复用接口与视觉验证；真实微信手机号授权、订阅消息、小程序发布、真实 OCR、生产数据库、研究角色/盲态访问范围及生产部署尚未联调。部分图标仍通过 Iconify 获取，业务数据虽完全本地，但不承诺所有静态图标完全离线；自动化测试与本次浏览器检查不代表覆盖所有交互组合。

## 研究管理

项目功能、分组页面、版本边界及最新验收结果统一维护在 [功能架构与版本规划](docs/结核病临床研究患者管理系统_功能架构.md) 第十一节。后续调整先更新该文档，再实施；本文件不再重复维护项目需求。

入口：研究管理 → 项目列表。列表显示分组数、去重患者数及按研究周期计算的状态，操作栏提供分组、编辑、状态及删除；“状态”仅用于手动结束项目，项目表单不再维护研究类型和备注，结束日期可往后调整。只有没有分组且没有患者的空项目可以删除，存在分组时先进入分组列表处理。新建分组使用基础信息弹窗并在成功后返回当前分组列表，分组列表“编辑”以弹窗只改名称和说明；没有患者的空分组可删除，组内未使用配置随分组删除，通用模板保留。“进入分组”后固定显示患者、用药方案、随访任务-问卷、随访任务-提醒四个页签；患者页只读展示已入组患者并提供详情入口，其他页签在配置模式中维护。全部继续使用本地 Mock。
